info:
  name: 5. Private info
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/items/psychicData"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
